apt: []

git<apt-install>:
  - apt

curl<apt-install>:
  - apt

gzip<apt-install>:
  - apt

shell:
  - zsh
  - oh-my-zsh
  - proxy

zsh<apt-install>:
  - apt

oh-my-zsh:
  - zsh
  - curl

proxy: []

pyenv:
  - curl
