apt: []

git<apt-install>:
  - apt

curl<apt-install>:
  - apt

gzip<apt-install>:
  - apt

shell:
  - zsh<apt-install>
  - ohmyzsh

zsh<apt-install>:
  - apt

ohmyzsh:
  - zsh
  - curl

proxy: []

pyenv:
  - curl<apt-install>
